# GENERATED by `nix run .#regenerate-manifests` from cluster.json. Do not edit.
# Netgear Switch
apiVersion: v1
kind: Service
metadata:
  name: netgearswitch
  namespace: external
spec:
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Endpoints
metadata:
  name: netgearswitch
  namespace: external
subsets:
  - addresses:
      - ip: 192.168.1.136
    ports:
      - port: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: netgearswitch
  namespace: external
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/controller: "none"
    traefik.ingress.kubernetes.io/router.middlewares: external-authelia@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: netgearswitch.homelab.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: netgearswitch
                port:
                  number: 80
  tls:
    - hosts:
        - netgearswitch.homelab.invalid
      secretName: netgearswitch-tls
